package boatsimulator;

public class BoatSimulator {

    public static void main(String[] args){
        new BoatSimulator();
        /*
        Future: add options before loading game up, 
        ideas in notes but also include 
            - resolution and button settings
            - hourglass mode (enemy always spawns in front of player, not randomly)
            - difficulty settings
            - boat type selection
            - solo or duo (just changes actions available while others are being done and makes some actions faster)
        */
        }

    public BoatSimulator() {
        new RunGame();
    }
}
